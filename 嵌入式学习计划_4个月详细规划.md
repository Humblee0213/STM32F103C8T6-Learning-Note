下面按你附件那种**纯文本、树状、直观思维导图**的形式整理。核心结论先说：

```text
main.c 不直接执行车辆控制逻辑
        ↓
启动定时器和外设
        ↓
500us中断负责“收数据”
        ↓
10ms中断负责“做决策、更新输出、发数据”
```

整个应用层可以概括为：

```text
10ms_int.c：路由
EMS_info.c：解析
Master_info.c：决策与控制中枢
```

---

# 一、从 `main()` 进入应用层的完整链路

入口文件：

[applilet/main.c](<D:\01学习资料\01嵌入式\02_集世迈VCU项目\JSMVCU2026.03.13 座椅通讯\applilet\main.c:131>)

```text
main()
  │
  ├─ Clock_Init()
  │    └─ 初始化系统时钟
  │
  ├─ SystemInit()
  │    └─ 初始化系统底层环境
  │
  ├─ start_up()
  │    │
  │    ├─ LVI_Start()
  │    │    └─ 低电压检测启动
  │    │
  │    ├─ AD0_Start()
  │    │    └─ ADC模块启动
  │    │
  │    └─ can_startup()
  │         ├─ CAN0_Enable()
  │         ├─ CAN1_Enable()
  │         ├─ CAN2_Enable()
  │         └─ CAN3_Enable()
  │
  ├─ TAA0_Start()
  │    └─ 启动500us定时器
  │
  ├─ TAA1_Start()
  │    └─ 启动10ms定时器
  │
  ├─ TAA3_Start()
  │    └─ 启动其他PWM/定时功能
  │
  ├─ TAA4_Start()
  │    └─ 启动行走PWM功能
  │
  ├─ EI()
  │    └─ 开启全局中断
  │
  └─ while(1)
       ├─ save_err_dat()
       │    └─ 后台保存故障数据
       │
       ├─ WDTE = 0xac
       │    └─ 喂狗，防止系统复位
       │
       └─ reset_boot_flag判断
            └─ 处理软件复位
```

`main()`启动完成之后，真正的应用控制不是通过 `while(1)` 直接调用，而是通过两个定时器中断进入。

---

# 二、整个应用层总架构图

```text
                    ┌─────────────────────────────┐
                    │        applilet/main.c       │
                    │          main()               │
                    └──────────────┬──────────────┘
                                   │
                     初始化外设、启动定时器、开中断
                                   │
                ┌──────────────────┴──────────────────┐
                │                                     │
                ▼                                     ▼
      ┌──────────────────┐                 ┌──────────────────┐
      │  TAA0 500us中断   │                 │  TAA1 10ms中断   │
      │ Timer0_500us.c    │                 │ Timer1_10ms.c    │
      └────────┬─────────┘                 └────────┬─────────┘
               │                                    │
               │ 收集输入数据                        │ 周期性执行控制
               │                                    │
               ▼                                    ▼
      ┌──────────────────┐                 ┌──────────────────┐
      │ CAN0~CAN3接收     │                 │ pro_AD_dat()     │
      │ ADC采样           │                 │ IOinGetState()   │
      └────────┬─────────┘                 │ SysControl()     │
               │                           │ CAN发送调度       │
               ▼                           └────────┬─────────┘
      ┌──────────────────┐                          │
      │ 10ms_int.c       │                          │
      │ 按CAN ID路由      │                          │
      └────────┬─────────┘                          │
               │                                    │
               ▼                                    │
      ┌──────────────────┐                          │
      │ EMS_info.c       │                          │
      │ CAN数据解析       │                          │
      │ 写入RxData结构体  │                          │
      └────────┬─────────┘                          │
               │                                    │
               └──────────────┬─────────────────────┘
                              ▼
                    ┌──────────────────┐
                    │ Master_info.c    │
                    │ 应用层控制中枢     │
                    │                  │
                    │ SysControl()     │
                    │ 状态机            │
                    │ 自动/手动控制      │
                    │ 制动压力计算       │
                    │ 行走/转向输出      │
                    └────────┬─────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
       CAN0/CAN1发送    CAN2线控制动      GPIO/PWM输出
       状态/执行器       0x303 → EHB       行走/方向/比例阀
```

---

# 三、500us中断：应用层的数据入口

文件：

[Timer0_500us.c](<D:\01学习资料\01嵌入式\02_集世迈VCU项目\JSMVCU2026.03.13 座椅通讯\Timer0_500us.c:193>)

```text
MD_INTTAA0CC0()
  │
  ├─ DI()
  │    └─ 暂时关闭中断，避免接收过程被打断
  │
  ├─ can0_receive()
  │    └─ 接收CAN0报文
  │
  ├─ can1_receive()
  │    └─ 接收CAN1报文
  │
  ├─ can2_receive()
  │    └─ 接收CAN2报文
  │
  ├─ can3_receive()
  │    └─ 接收CAN3报文
  │
  ├─ get_adc_data()
  │    └─ 读取ADC原始采样值
  │
  └─ EI()
       └─ 恢复中断
```

数据接收链路如下：

```text
CAN硬件接收到报文
        ↓
canX_receive()
        ↓
process_canX_receive_data()
        ↓
10ms_int.c中的pro_canX_dat()
        ↓
EMS_info.c中的pro_XXXRx()
        ↓
写入AP1RxData、AP2RxData、ST1RxData等结构体
        ↓
等待10ms控制周期读取
```

注意：

```text
10ms_int.c虽然名字叫“10ms中断”，
但其中的CAN路由函数实际上是被500us接收中断调用的。
```

它的主要职责不是定时，而是：

```text
根据CAN通道和CAN ID，把报文转给正确的解析函数。
```

---

# 四、10ms中断：应用层的控制调度入口

文件：

[Timer1_10ms.c](<D:\01学习资料\01嵌入式\02_集世迈VCU项目\JSMVCU2026.03.13 座椅通讯\Timer1_10ms.c:75>)

```text
MD_INTTAA1CC0()
  │
  ├─ time_cnt++
  │    └─ 10ms软件节拍计数器加1
  │
  ├─ pro_AD_dat()
  │    └─ 处理ADC采样结果
  │         ├─ 摇杆输入
  │         ├─ 手自动切换输入
  │         ├─ 转向角度反馈
  │         └─ 前进/后退输入
  │
  ├─ IOinGetState()
  │    └─ 读取并防抖处理数字量输入
  │         ├─ 前进
  │         ├─ 后退
  │         ├─ 各类限位
  │         ├─ 自动模式开关
  │         └─ 遥控模式开关
  │
  ├─ SysControl()
  │    └─ 应用层核心控制逻辑
  │         ├─ 判断workStatus
  │         ├─ 自动/手动模式控制
  │         ├─ AP1行走和转向
  │         ├─ AP2线控制动
  │         ├─ AP3比例阀
  │         ├─ EStop安全处理
  │         ├─ LIFE心跳监测
  │         └─ 准备下一帧发送数据
  │
  ├─ can0_send(time_cnt)
  │    └─ CAN0周期发送
  │
  ├─ can1_send(time_cnt)
  │    └─ CAN1周期发送整车状态
  │
  ├─ can2_send(time_cnt)
  │    └─ CAN2发送伺服等数据
  │
  ├─ can3_send(time_cnt)
  │    └─ CAN3发送其他执行器数据
  │
  ├─ can2_send_xkzd(time_cnt)
  │    └─ 每20ms发送一次线控制动0x303
  │
  └─ can_recover()
       └─ 检查并恢复CAN总线异常
```

最重要的执行顺序是：

```text
500us中断先更新输入数据
        ↓
10ms中断读取这些输入
        ↓
SysControl()做控制决策
        ↓
产生行走、转向、制动等输出变量
        ↓
CAN发送函数把输出变量发送给外部设备
```

---

# 五、Master_info.c：应用层控制中枢

文件：

[Master_info.c](<D:\01学习资料\01嵌入式\02_集世迈VCU项目\JSMVCU2026.03.13%20座椅通讯\user_app\Master_info.c>)

```text
Master_info.c
  │
  ├─ 一、输入数据处理
  │    │
  │    ├─ get_adc_data()
  │    │    └─ 获取ADC原始采样值
  │    │
  │    ├─ pro_AD_dat()
  │    │    └─ ADC数值转换为工程信号
  │    │         ├─ 摇杆
  │    │         ├─ 角度传感器
  │    │         ├─ 手自动输入
  │    │         └─ 前进/后退输入
  │    │
  │    └─ IOinGetState()
  │         └─ 数字IO采样、防抖、状态更新
  │
  ├─ 二、CAN接收底层封装
  │    │
  │    ├─ can0_receive()
  │    ├─ can1_receive()
  │    ├─ can2_receive()
  │    └─ can3_receive()
  │
  │    接收到报文后：
  │
  │    canX_receive()
  │        ↓
  │    process_canX_receive_data()
  │        ↓
  │    交给10ms_int.c路由
  │
  ├─ 三、SysControl()控制中枢
  │    │
  │    ├─ 读取ST1RxData、AP1RxData、AP2RxData等输入
  │    │
  │    ├─ 根据workStatus进入三态状态机
  │    │    ├─ 0：待机
  │    │    ├─ 1：正常
  │    │    └─ 2：故障
  │    │
  │    ├─ 正常态
  │    │    ├─ TJY=1：自动模式
  │    │    │    ├─ AP1行走
  │    │    │    ├─ AP1转向
  │    │    │    ├─ AP2制动
  │    │    │    ├─ AP3比例阀
  │    │    │    └─ AP1 LIFE监测
  │    │    │
  │    │    └─ TJY=0：手动模式
  │    │         ├─ 手动行走
  │    │         ├─ 手动转向
  │    │         ├─ 手动制动
  │    │         └─ 本地座椅控制
  │    │
  │    ├─ 故障态
  │    │    ├─ 停止转向输出
  │    │    ├─ 停止行走PWM
  │    │    ├─ 清零比例阀输出
  │    │    ├─ 施加制动
  │    │    └─ 执行故障恢复判断
  │    │
  │    └─ 输出控制变量计算
  │         ├─ Brake_AimPressure
  │         ├─ DUTY1b
  │         ├─ SFSend
  │         ├─ P5.0/P5.1/P5.4
  │         └─ 各比例阀开度
  │
  ├─ 四、输出报文封装
  │    │
  │    ├─ can0_pdusend()
  │    ├─ can1_pdusend()
  │    ├─ can2_pdusend()
  │    ├─ can3_pdusend()
  │    └─ can2_pdusend_xkzd()
  │
  ├─ 五、周期发送调度
  │    │
  │    ├─ can0_send()
  │    ├─ can1_send()
  │    ├─ can2_send()
  │    ├─ can3_send()
  │    └─ can2_send_xkzd()
  │
  └─ 六、辅助功能
       ├─ DLIM()
       │    └─ 对制动压力、速度等进行变化率限制
       │
       ├─ Flash读取接口
       │
       └─ 故障/状态相关辅助处理
```

一句话理解：

```text
Master_info.c负责把“输入数据”变成“控制输出”。
```

它是整个应用层的决策中心。

---

# 六、10ms_int.c：CAN报文路由层

文件：

[10ms_int.c](<D:\01学习资料\01嵌入式\02_集世迈VCU项目\JSMVCU2026.03.13%20座椅通讯\user_app\10ms_int.c>)

```text
10ms_int.c
  │
  ├─ pro_can0_dat()
  │    └─ 路由CAN0标准帧
  │         ├─ 0x0581 → pro_PV1Rx()
  │         ├─ 0x0582 → pro_PV2Rx()
  │         ├─ 0x0181 → pro_LX1Rx()
  │         ├─ 0x0583 → pro_PV3Rx()
  │         ├─ 0x0182 → pro_LX2Rx()
  │         └─ 0x018D → pro_ST1Rx()
  │
  ├─ pro_can1_dat()
  │    └─ 路由CAN1扩展帧
  │         ├─ 0x1801A060 → pro_AP1Rx()
  │         ├─ 0x1802A060 → pro_AP2Rx()
  │         └─ 0x1803A060 → pro_AP3Rx()
  │
  ├─ pro_can2_dat()
  │    └─ 路由CAN2标准帧
  │         ├─ 0x0581 → pro_ServoRx()
  │         └─ 0x0304 → pro_EHBRx()
  │
  └─ pro_can3_dat()
       └─ 路由CAN3标准帧
            ├─ 0x13C → pro_QT1Rx()
            ├─ 0x14C → pro_QT2Rx()
            └─ 0x17F → pro_QT3Rx()
```

它本身不负责复杂控制，也不负责判断自动还是手动。

它只做一件事：

```text
根据CAN通道 + CAN ID，
把一帧原始数据交给对应的解析函数。
```

可以把它理解为：

```text
CAN报文分拣中心
```

---

# 七、EMS_info.c：报文解析和数据缓存层

文件：

[EMS_info.c](<D:\01学习资料\01嵌入式\02_集世迈VCU项目\JSMVCU2026.03.13%20座椅通讯\user_app\EMS_info.c>)

```text
EMS_info.c
  │
  ├─ 接收数据结构体
  │    ├─ ST1RxData
  │    ├─ AP1RxData
  │    ├─ AP2RxData
  │    ├─ AP3RxData
  │    ├─ QT1RxData
  │    ├─ QT2RxData
  │    ├─ QT3RxData
  │    ├─ PV1RxData
  │    ├─ PV2RxData
  │    ├─ PV3RxData
  │    ├─ LX1RxData
  │    ├─ LX2RxData
  │    ├─ ServoRxData
  │    └─ EHBRxData
  │
  ├─ pro_ST1Rx()
  │    └─ CAN0 0x018D原始数据
  │         └─ 写入ST1RxData
  │              ├─ K11 → TJY手动/自动模式
  │              ├─ EStop → 急停状态
  │              ├─ D/R → 前进/后退
  │              └─ Seat1/Seat2等手动输入
  │
  ├─ pro_AP1Rx()
  │    └─ AP1自动驾驶报文
  │         └─ 写入AP1RxData
  │              ├─ SYS_MODE
  │              ├─ EN_AP
  │              ├─ DIR_AP
  │              ├─ 速度
  │              ├─ 转向角度
  │              └─ LIFE
  │
  ├─ pro_AP2Rx()
  │    └─ AP2自动制动报文
  │         └─ 写入AP2RxData
  │              ├─ BRAKE
  │              └─ 目标制动压力
  │
  ├─ pro_AP3Rx()
  │    └─ AP3比例阀/座椅执行器报文
  │
  ├─ pro_QT1Rx()/pro_QT2Rx()/pro_QT3Rx()
  │    └─ 解析行走、电池、油泵反馈
  │
  ├─ pro_PV1Rx()/pro_PV2Rx()/pro_PV3Rx()
  │    └─ 解析比例阀反馈
  │
  ├─ pro_LX1Rx()/pro_LX2Rx()
  │    └─ 解析位置/角度传感器反馈
  │
  ├─ pro_ServoRx()
  │    └─ 解析伺服反馈
  │         └─ 更新回零完成状态
  │
  └─ pro_EHBRx()
       └─ 解析EHB 0x304反馈
            └─ 保存实际制动压力等反馈数据
```

EMS_info.c中的大部分 `pro_XXXRx()` 逻辑比较简单，主要是：

```text
接收8字节CAN数据
        ↓
复制到对应RxData结构体
        ↓
Master_info.c通过位域或byte访问
```

因此：

```text
EMS_info.c负责“把报文变成软件可访问的数据”
Master_info.c负责“使用这些数据做控制决策”
```

---

# 八、三个核心报文的数据流示例

## 1. AP1自动驾驶控制链路

```text
CAN1
ID：0x1801A060
        ↓
can1_receive()
        ↓
process_can1_receive_data()
        ↓
pro_can1_dat()
        ↓
pro_AP1Rx()
        ↓
AP1RxData
        ↓
SysControl()
        ├─ 判断SYS_MODE
        ├─ 判断EN_AP
        ├─ 处理DIR_AP
        ├─ 计算行走速度
        ├─ 计算转向控制量
        └─ 监测LIFE心跳
        ↓
行走GPIO/PWM、转向输出
```

## 2. AP2线控制动链路

```text
CAN1
ID：0x1802A060
        ↓
pro_AP2Rx()
        ↓
AP2RxData
        ↓
SysControl()
        ├─ 判断BRAKE
        ├─ 提取目标制动压力
        ├─ 赋值target_Brake_AimPressure
        └─ 通过DLIM()限制压力变化率
        ↓
Brake_AimPressure
        ↓
can2_pdusend_xkzd()
        ↓
CAN2
ID：0x303
        ↓
EHB执行制动
```

## 3. ST1手动/安全控制链路

```text
CAN0
ID：0x018D
        ↓
pro_ST1Rx()
        ↓
ST1RxData
        ├─ K11 → TJY
        ├─ EStop → 急停状态
        ├─ D/R → 前进/后退
        └─ Seat1/Seat2等手动指令
        ↓
SysControl()
        ├─ TJY=0 → 手动控制
        ├─ EStop=0 → 进入故障态
        └─ EStop恢复 → 故障恢复判断
```

---

# 九、应用层中被省略的辅助文件

这些文件不作为当前阶段的重点深读对象，只需要知道职责。

```text
user_app/can_opt.c
  └─ CAN启动、CAN总线异常恢复、发送缓冲区清理

RelayControl.c
  └─ 继电器控制和继电器状态处理

flash_operate.c
  └─ Flash底层读写操作

user_app/data_save.c
  └─ 故障数据、配置数据保存

ADC_Temp.c
  └─ ADC或温度相关辅助处理

serial.c
  └─ 串口通信辅助功能

Master_info.h
  └─ Master_info.c的变量和函数声明

EMS_info.h
  └─ 各类CAN报文结构体、位域和RxData声明

10ms_int.h
  └─ 路由函数和中断相关声明
```

另外：

```text
applilet/
```

主要是工具生成的底层驱动层，包括：

```text
CAN驱动
ADC驱动
Timer驱动
GPIO/Port驱动
系统时钟驱动
```

它们属于：

```text
硬件驱动层
```

不是你当前需要深读的应用业务逻辑。

---

# 十、最终的应用层思维导图

```text
VCU应用层
  │
  ├─ 程序启动
  │    └─ main()
  │         ├─ 初始化时钟、ADC、CAN
  │         ├─ 启动TAA0、TAA1等定时器
  │         └─ 开启中断
  │
  ├─ 数据采集入口
  │    └─ 500us中断
  │         ├─ CAN0接收
  │         ├─ CAN1接收
  │         ├─ CAN2接收
  │         ├─ CAN3接收
  │         └─ ADC采样
  │
  ├─ 数据路由
  │    └─ 10ms_int.c
  │         ├─ 根据CAN0 ID路由
  │         ├─ 根据CAN1 ID路由
  │         ├─ 根据CAN2 ID路由
  │         └─ 根据CAN3 ID路由
  │
  ├─ 数据解析
  │    └─ EMS_info.c
  │         ├─ AP1解析
  │         ├─ AP2解析
  │         ├─ AP3解析
  │         ├─ ST1解析
  │         ├─ EHB解析
  │         └─ 各类反馈解析
  │
  ├─ 控制决策中枢
  │    └─ Master_info.c
  │         ├─ workStatus状态机
  │         ├─ TJY自动/手动判断
  │         ├─ 自动行走
  │         ├─ 自动转向
  │         ├─ 自动制动
  │         ├─ 手动控制
  │         ├─ EStop安全处理
  │         ├─ AP1 LIFE心跳监测
  │         └─ 输出变量计算
  │
  ├─ 控制输出
  │    └─ Master_info.c中的发送函数
  │         ├─ CAN0状态/执行器输出
  │         ├─ CAN1整车状态上报
  │         ├─ CAN2伺服输出
  │         ├─ CAN2 0x303线控制动
  │         ├─ CAN3执行器输出
  │         ├─ GPIO方向输出
  │         └─ PWM行走输出
  │
  └─ 辅助功能
       ├─ CAN总线恢复
       ├─ Flash故障保存
       ├─ 继电器控制
       └─ 串口调试
```

最值得你记住的一条主线是：

```text
main()
  → 定时器中断
  → CAN接收
  → 10ms_int.c路由
  → EMS_info.c解析
  → Master_info.c决策
  → CAN/GPIO/PWM输出
```

而三个核心文件的面试级概括是：

```text
10ms_int.c：解决“这帧报文应该交给谁处理”
EMS_info.c：解决“这帧报文里的数据如何保存和访问”
Master_info.c：解决“根据这些数据，VCU应该怎么控制车辆”
```
